## [ XML ]

### 핵심 한 줄 요약
    XML이 쓰이는 곳이 아직도 있으니 알아두면 쓰일 곳이 분명 있을 것 같다.

### 인트로
- 웹에서 규격화된 데이터를 효율적으로 주고받기 위해 만든 마크업 언어이다.
- JSON, YAML 보다 먼저 등장했다.

### 특징
- 문자열 인코딩을 직접 지정할 수 있다.:
  - 인코딩 정보가 파일 안에 있어서 인코딩 정보를 미리 알고 있어야 한다.
  - 근데 거의 UTF-8 이므로 장점으로 보기는 어렵다.
- HTML과 유사한 모양이다:
  - 태그를 열고 적고 닫고가 기본이다.
  - 맨 위에 HTML 명시 하는 것처럼 인코딩 규격 명시한다. => 권장
  - 동일한 태그를 여러번 작성하면 배열처럼 사용한다. => 반복자 식별자라고 한다.


### 구체적인 사용법은 사용할 때 검색해서 사용하자. 
